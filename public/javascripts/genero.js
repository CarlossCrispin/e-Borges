function showEliminar(valor) {
    var valor = valor;
    $('#eliminar').val(valor);
    // alert(valor);
}
function showID(valor) {
    var valor = valor;
    $('#ID').val(valor);
    
}
function showGenero(valor) {
    var valor = valor;
    $('#genero').val(valor);
    $("#generoS").text(valor);
}